{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"TelemetryKit","text":"<p>TelemetryKit (<code>tkit</code>) is a C++ telemetry framework for FRC robots. It provides structured logging with support for NetworkTables 4, WPILog files, and console output.</p>","path":["TelemetryKit"],"tags":[]},{"location":"#features","level":2,"title":"Features","text":"<ul> <li>Type-Safe - Compile-time type checking with <code>std::variant</code> storage</li> <li>Multiple Receivers - Log to NT4, WPILog files, and console simultaneously</li> <li>Struct Support - WPILib struct serialization with automatic schema publishing</li> <li>Field-Change-Only - Only logs values when they change to reduce overhead</li> </ul>","path":["TelemetryKit"],"tags":[]},{"location":"#quick-start","level":2,"title":"Quick Start","text":"","path":["TelemetryKit"],"tags":[]},{"location":"#installation","level":3,"title":"Installation","text":"<p>Using Vendor JSON (Recommended)</p> <ol> <li>Open VS Code with your FRC project</li> <li>Press <code>Ctrl+Shift+P</code> (or <code>Cmd+Shift+P</code> on Mac)</li> <li>Type \"WPILib: Manage Vendor Libraries\"</li> <li>Select \"Install new libraries (online)\"</li> <li>Paste this URL:</li> </ol> <pre><code>https://raw.githubusercontent.com/SticksDev/TelemetryKit/refs/heads/2026/TelemetryKit.json\n</code></pre> <p>Using Gradle (Alternative)</p> <p>Add JitPack repository to your <code>build.gradle</code>:</p> <pre><code>repositories {\n    maven { url 'https://jitpack.io' }\n}\n</code></pre> <p>Add the dependency:</p> <pre><code>dependencies {\n    implementation 'com.github.SticksDev:TelemetryKit:v1.0.0'\n}\n</code></pre>","path":["TelemetryKit"],"tags":[]},{"location":"#basic-usage","level":3,"title":"Basic Usage","text":"Robot.cpp<pre><code>#include &lt;telemetrykit/TelemetryKit.h&gt;\n\nvoid Robot::RobotInit() {\n  auto&amp; logger = tkit::Logger::GetInstance();\n\n  // Add receivers\n  logger.AddReceiver(std::make_unique&lt;tkit::NetworkTablesReceiver&gt;()); // (1)!\n  logger.AddReceiver(std::make_unique&lt;tkit::WPILogWriter&gt;(\"/home/lvuser/logs\")); // (2)!\n\n  logger.Start(); // (3)!\n}\n\nvoid Robot::RobotPeriodic() {\n  auto&amp; logger = tkit::Logger::GetInstance();\n\n  // Log primitive values (using member functions)\n  logger.RecordOutput(\"DriveTrain/Speed\", 3.5); // (4)!\n  logger.RecordOutput(\"DriveTrain/Enabled\", true);\n\n  // Or use global convenience functions\n  tkit::RecordOutput(\"DriveTrain/Current\", 12.5); // (5)!\n\n  // Log structs (automatically handles schema publishing)\n  frc::Pose2d pose{1.5_m, 2.3_m, frc::Rotation2d{45_deg}};\n  logger.RecordOutput(\"RobotPose\", tkit::MakeStructValue(pose)); // (6)!\n\n  // Update logger (sends data to all receivers)\n  logger.Periodic(); // (7)!\n}\n</code></pre> <ol> <li>Publishes to NT4 for real-time viewing in Shuffleboard, Glass, or AdvantageScope</li> <li>Writes timestamped <code>.wpilog</code> files to <code>/home/lvuser/logs/</code></li> <li>Initializes all receivers (calls <code>OnStart()</code> on each)</li> <li>Logger member function - call on the logger instance</li> <li>Global convenience function - equivalent to <code>Logger::GetInstance().RecordOutput(...)</code></li> <li>Serializes struct and collects all schemas (including nested Translation2d, Rotation2d)</li> <li>Sends all logged data to receivers (calls <code>OnUpdate()</code> on each)</li> </ol> <p>Member Functions vs Global Functions</p> <p>Both approaches work identically - use <code>logger.RecordOutput(...)</code> for member functions or <code>tkit::RecordOutput(...)</code> for global convenience functions. Choose whichever style you prefer!</p> <p>Call Periodic() Every Cycle</p> <p><code>Logger::Periodic()</code> (or <code>tkit::Periodic()</code>) must be called in <code>RobotPeriodic()</code> to flush data to all receivers.</p>","path":["TelemetryKit"],"tags":[]},{"location":"#architecture","level":2,"title":"Architecture","text":"<pre><code>graph LR\n  A[Logger] --&gt; B[LogTable]\n  B --&gt; C[NetworkTablesReceiver]\n  B --&gt; D[WPILogWriter]\n  B --&gt; E[ConsoleReceiver]\n  C --&gt; F[NT4 Client]\n  D --&gt; G[.wpilog File]\n  E --&gt; H[Console]</code></pre> <p>The Logger manages a LogTable (key-value store) and distributes updates to multiple receivers. Each receiver processes the data in its own way:</p> <ul> <li>NetworkTablesReceiver - Publishes to NT4 for real-time dashboard viewing</li> <li>WPILogWriter - Saves to .wpilog files for post-match analysis</li> <li>ConsoleReceiver - Prints to console for debugging</li> </ul>","path":["TelemetryKit"],"tags":[]},{"location":"#next-steps","level":2,"title":"Next Steps","text":"<ul> <li>Core Concepts - Learn about Logger, LogValue, and Receivers</li> <li>Receivers - Configure output destinations</li> <li>Struct Logging - Working with WPILib geometry types</li> <li>Examples - Common usage patterns</li> </ul>","path":["TelemetryKit"],"tags":[]},{"location":"#design","level":2,"title":"Design","text":"<ul> <li>Zero-copy where possible - Minimal allocations during periodic calls</li> <li>Field-change-only - Only log values when they change</li> <li>Type-safe - Catch type errors at compile time</li> <li>Schema-aware - Automatic struct schema publishing for NT4 and WPILog</li> </ul> <p>Get Started →</p>","path":["TelemetryKit"],"tags":[]},{"location":"advanced/structs/","level":1,"title":"Struct Logging","text":"<p>TelemetryKit supports WPILib struct serialization with automatic schema publishing to NetworkTables and WPILog files.</p> <p>Beta Feature</p> <p>Struct logging is in beta. Some structs may not work correctly. Use the helper functions (e.g., <code>LogPose2d()</code>, <code>LogSwerveModuleStates()</code>) for more reliable decomposed logging. </p> <p>Helper functions decompose structs into individual primitive fields, avoiding serialization issues and providing better dashboard compatibility.</p>","path":["Advanced","Struct Logging"],"tags":[]},{"location":"advanced/structs/#quick-start","level":2,"title":"Quick Start","text":"<pre><code>#include &lt;frc/geometry/Pose2d.h&gt;\n#include &lt;telemetrykit/TelemetryKit.h&gt;\n\n// Log a Pose2d\nfrc::Pose2d robotPose{1.5_m, 2.3_m, frc::Rotation2d{45_deg}};\ntkit::Logger::RecordOutput(\"Odometry/Pose\", tkit::MakeStructValue(robotPose));\n\n// Log an array of SwerveModuleStates\nstd::array&lt;frc::SwerveModuleState, 4&gt; moduleStates = {...};\ntkit::Logger::RecordOutput(\"Drive/ModuleStates\",\n  tkit::MakeStructArrayValue(std::span(moduleStates)));\n</code></pre> <p>TelemetryKit automatically publishes struct schemas to NetworkTables and encodes them correctly in WPILog files.</p>","path":["Advanced","Struct Logging"],"tags":[]},{"location":"advanced/structs/#how-it-works","level":2,"title":"How It Works","text":"","path":["Advanced","Struct Logging"],"tags":[]},{"location":"advanced/structs/#serialization","level":3,"title":"Serialization","text":"<p>When you call <code>MakeStructValue()</code>, TelemetryKit:</p> <ol> <li>Serializes the struct to bytes using <code>wpi::Struct</code></li> <li>Extracts the type name (e.g., \"Pose2d\")</li> <li>Collects schema bytes using <code>wpi::GetStructSchemaBytes&lt;T&gt;()</code></li> <li>Gathers nested struct schemas (e.g., Translation2d, Rotation2d for Pose2d)</li> </ol> <pre><code>graph LR\n    A[Pose2d Object] --&gt;|MakeStructValue| B[Serialize to bytes]\n    B --&gt; C[Extract type name]\n    C --&gt; D[Get schema bytes]\n    D --&gt; E[Collect nested schemas]\n    E --&gt; F[LogValue with all schemas]</code></pre>","path":["Advanced","Struct Logging"],"tags":[]},{"location":"advanced/structs/#schema-publishing","level":3,"title":"Schema Publishing","text":"","path":["Advanced","Struct Logging"],"tags":[]},{"location":"advanced/structs/#networktables","level":4,"title":"NetworkTables","text":"<p>When <code>NetworkTablesReceiver</code> encounters a struct for the first time:</p> <pre><code>// Publishes schemas for Pose2d and its nested structs\nm_inst.AddSchema(\"Pose2d\", \"structschema\", pose2dSchemaBytes);\nm_inst.AddSchema(\"Translation2d\", \"structschema\", translation2dSchemaBytes);\nm_inst.AddSchema(\"Rotation2d\", \"structschema\", rotation2dSchemaBytes);\n\n// Then publishes the data as a RawTopic\nauto topic = m_inst.GetRawTopic(\"/Odometry/Pose\");\nauto publisher = topic.Publish(\"Pose2d\");  // (1)!\npublisher.Set(serializedBytes, timestamp);\n</code></pre> <ol> <li>The type string \"Pose2d\" tells NT4 clients which schema to use for deserialization</li> </ol>","path":["Advanced","Struct Logging"],"tags":[]},{"location":"advanced/structs/#wpilog-files","level":4,"title":"WPILog Files","text":"<p><code>WPILogWriter</code> creates log entries with the correct type string:</p> <pre><code>// Creates entry with \"struct:Pose2d\" type\nint entryId = m_log-&gt;Start(\"/Odometry/Pose\", \"struct:Pose2d\");\n\n// Appends serialized bytes\nm_log-&gt;AppendRaw(entryId, serializedBytes, timestamp);\n</code></pre> <p>Tools like AdvantageScope can then deserialize the struct data for visualization.</p>","path":["Advanced","Struct Logging"],"tags":[]},{"location":"advanced/structs/#nested-struct-dependencies","level":2,"title":"Nested Struct Dependencies","text":"<p>TelemetryKit automatically handles nested struct dependencies using <code>wpi::ForEachStructSchema&lt;T&gt;()</code>:</p> <pre><code>// When you log a Pose2d\ntkit::MakeStructValue(pose);\n\n// TelemetryKit collects schemas for:\n// - Pose2d (the main struct)\n// - Translation2d (nested in Pose2d)\n// - Rotation2d (nested in Pose2d)\n</code></pre> <p>Without nested schemas, clients cannot deserialize structs that contain other structs. With nested schemas, clients receive all required schemas for full deserialization.</p>","path":["Advanced","Struct Logging"],"tags":[]},{"location":"advanced/structs/#supported-structs","level":2,"title":"Supported Structs","text":"<p>Any WPILib struct that implements <code>wpi::Struct&lt;T&gt;</code> is supported:</p>","path":["Advanced","Struct Logging"],"tags":[]},{"location":"advanced/structs/#geometry-structs","level":3,"title":"Geometry Structs","text":"<pre><code>#include &lt;frc/geometry/Pose2d.h&gt;\n#include &lt;frc/geometry/Pose3d.h&gt;\n#include &lt;frc/geometry/Translation2d.h&gt;\n#include &lt;frc/geometry/Translation3d.h&gt;\n#include &lt;frc/geometry/Rotation2d.h&gt;\n#include &lt;frc/geometry/Rotation3d.h&gt;\n#include &lt;frc/geometry/Transform2d.h&gt;\n#include &lt;frc/geometry/Transform3d.h&gt;\n\ntkit::Logger::RecordOutput(\"Pose2D\", tkit::MakeStructValue(pose2d));\ntkit::Logger::RecordOutput(\"Pose3D\", tkit::MakeStructValue(pose3d));\n</code></pre>","path":["Advanced","Struct Logging"],"tags":[]},{"location":"advanced/structs/#kinematics-structs","level":3,"title":"Kinematics Structs","text":"<pre><code>#include &lt;frc/kinematics/SwerveModuleState.h&gt;\n#include &lt;frc/kinematics/SwerveModulePosition.h&gt;\n#include &lt;frc/kinematics/ChassisSpeeds.h&gt;\n\ntkit::Logger::RecordOutput(\"ModuleStates\",\n  tkit::MakeStructArrayValue(moduleStates));\n</code></pre>","path":["Advanced","Struct Logging"],"tags":[]},{"location":"advanced/structs/#custom-structs","level":3,"title":"Custom Structs","text":"<p>You can use any custom struct that implements <code>wpi::Struct&lt;T&gt;</code>:</p> <pre><code>// Your custom struct with wpi::Struct implementation\nstruct CustomData {\n  double value1;\n  int value2;\n\n  using StructType = wpi::Struct&lt;CustomData&gt;;\n  static constexpr std::string_view GetTypeName() { return \"CustomData\"; }\n  // ... implement required methods\n};\n\nCustomData data{1.5, 42};\ntkit::Logger::RecordOutput(\"Custom\", tkit::MakeStructValue(data));\n</code></pre>","path":["Advanced","Struct Logging"],"tags":[]},{"location":"advanced/structs/#struct-helper-functions","level":2,"title":"Struct Helper Functions","text":"<p>For dashboard visibility, TelemetryKit provides helpers to decompose structs into individual fields:</p> <pre><code>#include &lt;telemetrykit/core/StructLogger.h&gt;\n\nfrc::Pose2d pose{1.5_m, 2.3_m, frc::Rotation2d{45_deg}};\n\n// Instead of logging as a binary struct\ntkit::Logger::RecordOutput(\"Pose\", tkit::MakeStructValue(pose));\n\n// Decompose into individual fields for plotting\nauto&amp; table = tkit::Logger::GetTable();\ntkit::LogPose2d(table, \"Pose\", pose);\n// Creates:\n//   \"Pose/X\" -&gt; 1.5\n//   \"Pose/Y\" -&gt; 2.3\n//   \"Pose/Rotation\" -&gt; 0.785398 (45° in radians)\n</code></pre> <p>Available helpers:</p> Function Struct Type Fields Created <code>LogPose2d</code> <code>frc::Pose2d</code> X, Y, Rotation <code>LogPose3d</code> <code>frc::Pose3d</code> X, Y, Z, Rotation (Quaternion) <code>LogTranslation2d</code> <code>frc::Translation2d</code> X, Y <code>LogTranslation3d</code> <code>frc::Translation3d</code> X, Y, Z <code>LogSwerveModuleState</code> <code>frc::SwerveModuleState</code> Speed, Angle <code>LogSwerveModulePosition</code> <code>frc::SwerveModulePosition</code> Distance, Angle <code>LogSwerveModuleStates</code> <code>std::vector&lt;...&gt;</code> Module0/..., Module1/..., etc. <p>Use structs for full type preservation in AdvantageScope. Use helpers for individual fields that can be plotted on dashboards.</p>","path":["Advanced","Struct Logging"],"tags":[]},{"location":"advanced/structs/#performance-considerations","level":2,"title":"Performance Considerations","text":"","path":["Advanced","Struct Logging"],"tags":[]},{"location":"advanced/structs/#serialization-overhead","level":3,"title":"Serialization Overhead","text":"<p>Struct serialization has minimal overhead:</p> <pre><code>frc::Pose2d pose{...};\n\n// ~500 nanoseconds on roboRIO\nauto logValue = tkit::MakeStructValue(pose);  // (1)!\n</code></pre> <ol> <li>Includes serialization, schema lookup, and LogValue construction</li> </ol>","path":["Advanced","Struct Logging"],"tags":[]},{"location":"advanced/structs/#schema-publishing_1","level":3,"title":"Schema Publishing","text":"<p>Schemas are only published once per struct type:</p> <pre><code>// First Pose2d - publishes schemas\ntkit::Logger::RecordOutput(\"Pose1\", tkit::MakeStructValue(pose1));\ntkit::Logger::Periodic();  // → Publishes 3 schemas\n\n// Second Pose2d - skips schema publishing\ntkit::Logger::RecordOutput(\"Pose2\", tkit::MakeStructValue(pose2));\ntkit::Logger::Periodic();  // → Only publishes data\n</code></pre> <p>Schema publishing is a one-time cost per struct type, not per value.</p>","path":["Advanced","Struct Logging"],"tags":[]},{"location":"advanced/structs/#examples","level":2,"title":"Examples","text":"","path":["Advanced","Struct Logging"],"tags":[]},{"location":"advanced/structs/#complete-swerve-drive-telemetry","level":3,"title":"Complete Swerve Drive Telemetry","text":"<pre><code>#include &lt;telemetrykit/TelemetryKit.h&gt;\n#include &lt;telemetrykit/core/StructLogger.h&gt;\n#include &lt;frc/kinematics/SwerveModuleState.h&gt;\n\nclass SwerveDrive {\n public:\n  void UpdateTelemetry() {\n    auto&amp; table = tkit::Logger::GetTable();\n\n    // Log robot pose (full struct for AdvantageScope)\n    tkit::Logger::RecordOutput(\"Swerve/Pose\",\n      tkit::MakeStructValue(m_poseEstimator.GetEstimatedPosition()));\n\n    // Log module states (decomposed for dashboard)\n    tkit::LogSwerveModuleStates(table, \"Swerve/ModuleStates\", m_moduleStates);\n\n    // Log as struct array for AdvantageScope\n    tkit::Logger::RecordOutput(\"Swerve/ModuleStatesStruct\",\n      tkit::MakeStructArrayValue(std::span(m_moduleStates)));\n  }\n\n private:\n  std::array&lt;frc::SwerveModuleState, 4&gt; m_moduleStates;\n  frc::SwerveDrivePoseEstimator m_poseEstimator;\n};\n</code></pre> <p>This creates the following NT4 topics and log entries:</p> <pre><code>/Swerve/Pose                            → Pose2d struct\n/Swerve/ModuleStates/Module0/Speed      → double\n/Swerve/ModuleStates/Module0/Angle      → double\n/Swerve/ModuleStates/Module1/Speed      → double\n/Swerve/ModuleStates/Module1/Angle      → double\n...\n/Swerve/ModuleStatesStruct              → SwerveModuleState[] struct array\n</code></pre> <p>Learn More About Receivers →</p>","path":["Advanced","Struct Logging"],"tags":[]},{"location":"core/logvalue/","level":1,"title":"LogValue","text":"<p>Type-safe container that can hold any loggable value using <code>std::variant</code>.</p>","path":["Core Concepts","LogValue"],"tags":[]},{"location":"core/logvalue/#overview","level":2,"title":"Overview","text":"<p>Provides:</p> <ul> <li>Compile-time safety - Template methods catch type errors at compile time</li> <li>Runtime flexibility - Can store any supported type in a single container</li> <li>Zero-copy where possible - Efficient storage and retrieval</li> <li>Automatic type detection - Templates deduce types from arguments</li> </ul>","path":["Core Concepts","LogValue"],"tags":[]},{"location":"core/logvalue/#supported-types","level":2,"title":"Supported Types","text":"","path":["Core Concepts","LogValue"],"tags":[]},{"location":"core/logvalue/#primitives","level":3,"title":"Primitives","text":"BooleanIntegerFloatDoubleString <pre><code>tkit::LogValue boolVal(true);\ntkit::Logger::RecordOutput(\"System/Enabled\", true);\n</code></pre> <pre><code>tkit::LogValue intVal(42);  // (1)!\ntkit::Logger::RecordOutput(\"Encoder/Ticks\", 1234);\n\n// Most integer types work\nint count = 10;\nlong bigNum = 1000000L;\ntkit::Logger::RecordOutput(\"Counter\", count);  // (2)!\ntkit::Logger::RecordOutput(\"BigValue\", bigNum);\n</code></pre> <ol> <li>Accepts <code>int</code>, <code>long</code>, <code>int64_t</code>, and most other integer primitives</li> <li>All integers are stored internally as <code>int64_t</code></li> </ol> <pre><code>tkit::LogValue floatVal(3.14f);\ntkit::Logger::RecordOutput(\"Motor/Voltage\", 12.5f);\n</code></pre> <pre><code>tkit::LogValue doubleVal(2.718);\ntkit::Logger::RecordOutput(\"Odometry/X\", 1.5);\n</code></pre> <pre><code>tkit::LogValue stringVal(\"Autonomous Mode\");\ntkit::Logger::RecordOutput(\"Auto/Selected\", std::string(\"3 Ball Auto\"));\n\n// String views work too\nstd::string_view mode = \"Test\";\ntkit::Logger::RecordOutput(\"Robot/Mode\", std::string(mode));\n</code></pre>","path":["Core Concepts","LogValue"],"tags":[]},{"location":"core/logvalue/#arrays","level":3,"title":"Arrays","text":"<p>All array types use <code>std::vector</code> and support dynamic sizing.</p> Boolean ArrayInteger ArrayFloat ArrayDouble ArrayString Array <pre><code>std::vector&lt;bool&gt; flags = {true, false, true, true};\ntkit::LogValue arrayVal(flags);\ntkit::Logger::RecordOutput(\"Subsystems/Enabled\", flags);\n</code></pre> <pre><code>std::vector&lt;int64_t&gt; encoderValues = {100, 200, 300, 400};\ntkit::Logger::RecordOutput(\"Swerve/EncoderTicks\", encoderValues);\n</code></pre> <pre><code>std::vector&lt;float&gt; voltages = {12.1f, 11.9f, 12.0f, 12.2f};\ntkit::Logger::RecordOutput(\"Motors/Voltages\", voltages);\n</code></pre> <pre><code>std::vector&lt;double&gt; wheelSpeeds = {1.5, 1.6, 1.4, 1.5};\ntkit::Logger::RecordOutput(\"Drive/WheelSpeeds\", wheelSpeeds);\n</code></pre> <pre><code>std::vector&lt;std::string&gt; moduleNames = {\"FL\", \"FR\", \"BL\", \"BR\"};\ntkit::Logger::RecordOutput(\"Swerve/ModuleNames\", moduleNames);\n</code></pre>","path":["Core Concepts","LogValue"],"tags":[]},{"location":"core/logvalue/#structs","level":3,"title":"Structs","text":"<p>WPILib structs are fully supported with automatic schema publishing.</p> Single StructStruct Array <pre><code>#include &lt;frc/geometry/Pose2d.h&gt;\n#include &lt;telemetrykit/core/StructLogger.h&gt;\n\nfrc::Pose2d pose{1.5_m, 2.3_m, frc::Rotation2d{45_deg}};\n\n// Use MakeStructValue helper\ntkit::LogValue structVal = tkit::MakeStructValue(pose);\ntkit::Logger::RecordOutput(\"Robot/Pose\", tkit::MakeStructValue(pose));\n</code></pre> <pre><code>#include &lt;frc/kinematics/SwerveModuleState.h&gt;\n#include &lt;telemetrykit/core/StructLogger.h&gt;\n\nstd::vector&lt;frc::SwerveModuleState&gt; states = {\n  {1.5_mps, frc::Rotation2d{0_deg}},\n  {1.6_mps, frc::Rotation2d{90_deg}},\n  {1.4_mps, frc::Rotation2d{180_deg}},\n  {1.5_mps, frc::Rotation2d{270_deg}}\n};\n\n// Use MakeStructArrayValue helper\ntkit::Logger::RecordOutput(\"Swerve/States\",\n  tkit::MakeStructArrayValue(std::span(states)));\n</code></pre> <p>See Struct Logging for comprehensive struct support details.</p>","path":["Core Concepts","LogValue"],"tags":[]},{"location":"core/logvalue/#raw-binary-data","level":3,"title":"Raw Binary Data","text":"<p>For custom binary data or unsupported types:</p> <pre><code>std::vector&lt;uint8_t&gt; rawData = {0x01, 0x02, 0x03, 0x04};\ntkit::LogValue rawVal(rawData, false);  // false = not a struct\ntkit::Logger::RecordOutput(\"Custom/Data\", rawVal);\n</code></pre>","path":["Core Concepts","LogValue"],"tags":[]},{"location":"core/logvalue/#type-system","level":2,"title":"Type System","text":"","path":["Core Concepts","LogValue"],"tags":[]},{"location":"core/logvalue/#logtype-enum","level":3,"title":"LogType Enum","text":"<pre><code>enum class LogType {\n  kBoolean,        // Single bool\n  kInt64,          // Single int64_t\n  kFloat,          // Single float\n  kDouble,         // Single double\n  kString,         // Single std::string\n\n  kBooleanArray,   // std::vector&lt;bool&gt;\n  kInt64Array,     // std::vector&lt;int64_t&gt;\n  kFloatArray,     // std::vector&lt;float&gt;\n  kDoubleArray,    // std::vector&lt;double&gt;\n  kStringArray,    // std::vector&lt;std::string&gt;\n\n  kRaw,            // std::vector&lt;uint8_t&gt; (binary data)\n  kStruct,         // Serialized WPILib struct\n  kStructArray     // Array of serialized structs\n};\n</code></pre>","path":["Core Concepts","LogValue"],"tags":[]},{"location":"core/logvalue/#runtime-type-information","level":3,"title":"Runtime Type Information","text":"<p>Every LogValue knows its type at runtime:</p> <pre><code>tkit::LogValue value(42);\n\n// Check the type\ntkit::LogType type = value.GetType();  // Returns LogType::kInt64\n\n// Type checking\nif (value.Is&lt;int64_t&gt;()) {\n  // Safe to retrieve as int64_t\n}\n\n// Get type as string\nstd::string typeStr = value.GetTypeString();  // Returns \"int64\"\n\n// For structs, returns the struct type name\nfrc::Pose2d pose{1_m, 2_m, 0_deg};\nauto structVal = tkit::MakeStructValue(pose);\nstd::string structType = structVal.GetTypeString();  // Returns \"Pose2d\"\n</code></pre>","path":["Core Concepts","LogValue"],"tags":[]},{"location":"core/logvalue/#retrieving-values","level":2,"title":"Retrieving Values","text":"","path":["Core Concepts","LogValue"],"tags":[]},{"location":"core/logvalue/#type-safe-retrieval","level":3,"title":"Type-Safe Retrieval","text":"<p>Use the <code>Get&lt;T&gt;()</code> template method to retrieve values:</p> <pre><code>tkit::LogValue value(3.14);\n\n// Retrieve the value (type must match exactly)\ndouble d = value.Get&lt;double&gt;();\n\n// Check type before retrieving to avoid exceptions\nif (value.Is&lt;double&gt;()) {\n  double d = value.Get&lt;double&gt;();\n  std::cout &lt;&lt; \"Value: \" &lt;&lt; d &lt;&lt; std::endl;\n}\n</code></pre> <p>Type Mismatch Throws Exception</p> <p>Calling <code>Get&lt;T&gt;()</code> with the wrong type throws <code>std::bad_variant_access</code>. Always check the type first with <code>Is&lt;T&gt;()</code> or <code>GetType()</code>.</p>","path":["Core Concepts","LogValue"],"tags":[]},{"location":"core/logvalue/#common-retrieval-patterns","level":3,"title":"Common Retrieval Patterns","text":"<pre><code>// Pattern 1: Check type enum\nif (value.GetType() == tkit::LogType::kDouble) {\n  double d = value.Get&lt;double&gt;();\n}\n\n// Pattern 2: Check with template\nif (value.Is&lt;std::vector&lt;double&gt;&gt;()) {\n  const auto&amp; array = value.Get&lt;std::vector&lt;double&gt;&gt;();\n  for (double val : array) {\n    // Process array values\n  }\n}\n\n// Pattern 3: Switch on type\nswitch (value.GetType()) {\n  case tkit::LogType::kDouble:\n    ProcessDouble(value.Get&lt;double&gt;());\n    break;\n  case tkit::LogType::kDoubleArray:\n    ProcessDoubleArray(value.Get&lt;std::vector&lt;double&gt;&gt;());\n    break;\n  // ... other cases\n}\n</code></pre>","path":["Core Concepts","LogValue"],"tags":[]},{"location":"core/logvalue/#value-comparison","level":2,"title":"Value Comparison","text":"<p>LogValue supports equality comparison:</p> <pre><code>tkit::LogValue a(3.14);\ntkit::LogValue b(3.14);\ntkit::LogValue c(2.71);\n\nbool same = (a == b);      // true\nbool different = (a == c); // false\n\n// Works with all types\ntkit::LogValue arr1(std::vector&lt;double&gt;{1.0, 2.0});\ntkit::LogValue arr2(std::vector&lt;double&gt;{1.0, 2.0});\nbool arrSame = (arr1 == arr2);  // true\n</code></pre> <p>Used internally for field-change-only optimization - receivers only log values when they change.</p>","path":["Core Concepts","LogValue"],"tags":[]},{"location":"core/logvalue/#custom-type-mapping","level":2,"title":"Custom Type Mapping","text":"","path":["Core Concepts","LogValue"],"tags":[]},{"location":"core/logvalue/#type-string-for-structs","level":3,"title":"Type String for Structs","text":"<p>When creating struct LogValues, provide the WPILib type name:</p> <pre><code>// MakeStructValue automatically extracts type name\nauto pose = frc::Pose2d{1_m, 2_m, 0_deg};\nauto value = tkit::MakeStructValue(pose);  // Type string = \"Pose2d\"\n\n// Manual construction (not recommended)\nstd::vector&lt;uint8_t&gt; serialized = /* ... */;\ntkit::LogValue value(serialized, \"Pose2d\");\n</code></pre>","path":["Core Concepts","LogValue"],"tags":[]},{"location":"core/logvalue/#schema-storage","level":3,"title":"Schema Storage","text":"<p>Struct LogValues store their schema for automatic publishing:</p> <pre><code>auto pose = frc::Pose2d{1_m, 2_m, 0_deg};\nauto value = tkit::MakeStructValue(pose);\n\n// Schema automatically included\nbool hasSchema = value.HasSchema();  // true\nconst auto&amp; schema = value.GetSchema();\n\n// Nested schemas (e.g., Pose2d includes Translation2d, Rotation2d)\nconst auto&amp; allSchemas = value.GetAllSchemas();\n// Returns: {\"Pose2d\" -&gt; schema, \"Translation2d\" -&gt; schema, \"Rotation2d\" -&gt; schema}\n</code></pre> <p>See Struct Logging for details on schema handling.</p>","path":["Core Concepts","LogValue"],"tags":[]},{"location":"core/logvalue/#performance-considerations","level":2,"title":"Performance Considerations","text":"","path":["Core Concepts","LogValue"],"tags":[]},{"location":"core/logvalue/#move-semantics","level":3,"title":"Move Semantics","text":"<p>LogValue supports move semantics for efficient transfers:</p> <pre><code>std::vector&lt;double&gt; largeArray(10000, 3.14);\n\n// Efficient: moves the vector into LogValue (no copy)\ntkit::LogValue value(std::move(largeArray));\n// largeArray is now empty\n\n// Also works with RecordOutput\nstd::vector&lt;double&gt; data(10000);\ntkit::Logger::RecordOutput(\"Data\", std::move(data));\n</code></pre>","path":["Core Concepts","LogValue"],"tags":[]},{"location":"core/logvalue/#small-value-optimization","level":3,"title":"Small Value Optimization","text":"<p>Primitive types (bool, int64_t, float, double) are stored directly in the variant without heap allocation:</p> <pre><code>// No heap allocation\ntkit::LogValue intVal(42);\ntkit::LogValue floatVal(3.14f);\ntkit::LogValue boolVal(true);\n\n// Single heap allocation for vector\ntkit::LogValue arrayVal(std::vector&lt;double&gt;{1.0, 2.0, 3.0});\n</code></pre>","path":["Core Concepts","LogValue"],"tags":[]},{"location":"core/logvalue/#copy-avoidance","level":3,"title":"Copy Avoidance","text":"<p>Use references to avoid copying when retrieving arrays:</p> <pre><code>tkit::LogValue value(std::vector&lt;double&gt;{1.0, 2.0, 3.0});\n\n// Bad: copies the entire vector\nauto copy = value.Get&lt;std::vector&lt;double&gt;&gt;();\n\n// Good: const reference, no copy\nconst auto&amp; ref = value.Get&lt;std::vector&lt;double&gt;&gt;();\n</code></pre>","path":["Core Concepts","LogValue"],"tags":[]},{"location":"core/logvalue/#type-conversion-table","level":2,"title":"Type Conversion Table","text":"C++ Type LogType Storage Notes <code>bool</code> <code>kBoolean</code> Direct No heap allocation <code>int</code>, <code>long</code>, <code>int64_t</code> <code>kInt64</code> Direct All integers stored as int64_t <code>float</code> <code>kFloat</code> Direct No heap allocation <code>double</code> <code>kDouble</code> Direct No heap allocation <code>std::string</code> <code>kString</code> Heap String contents on heap <code>std::vector&lt;bool&gt;</code> <code>kBooleanArray</code> Heap <code>std::vector&lt;int64_t&gt;</code> <code>kInt64Array</code> Heap <code>std::vector&lt;float&gt;</code> <code>kFloatArray</code> Heap <code>std::vector&lt;double&gt;</code> <code>kDoubleArray</code> Heap <code>std::vector&lt;std::string&gt;</code> <code>kStringArray</code> Heap <code>frc::Pose2d</code> <code>kStruct</code> Heap Via <code>MakeStructValue()</code> <code>frc::SwerveModuleState</code> <code>kStruct</code> Heap Via <code>MakeStructValue()</code> <code>std::vector&lt;frc::Pose2d&gt;</code> <code>kStructArray</code> Heap Via <code>MakeStructArrayValue()</code> <p>Incomplete Table</p> <p>This table is not complete. Most C++ primitive types (int, short, unsigned, etc.) are automatically converted to the listed types. Any WPILib struct that implements <code>wpi::Struct&lt;T&gt;</code> can be logged using <code>MakeStructValue()</code> or <code>MakeStructArrayValue()</code> for  lists.</p>","path":["Core Concepts","LogValue"],"tags":[]},{"location":"core/logvalue/#next-steps","level":2,"title":"Next Steps","text":"<ul> <li>Learn about Receivers that consume LogValues</li> <li>See Struct Logging for working with WPILib geometry types</li> <li>Check out Examples for practical usage patterns</li> </ul> <p>Receivers →</p>","path":["Core Concepts","LogValue"],"tags":[]},{"location":"core/overview/","level":1,"title":"Core Concepts","text":"<p>TelemetryKit has three main components: Logger, LogValue, and Receivers.</p>","path":["Core Concepts"],"tags":[]},{"location":"core/overview/#architecture-overview","level":2,"title":"Architecture Overview","text":"<pre><code>graph TD\n    A[Your Robot Code] --&gt;|RecordOutput| B[Logger Singleton]\n    B --&gt;|Stores in| C[LogTable]\n    C --&gt;|OnUpdate| D[NetworkTablesReceiver]\n    C --&gt;|OnUpdate| E[WPILogWriter]\n    C --&gt;|OnUpdate| F[ConsoleReceiver]\n    D --&gt;|Publishes| G[NT4 Topics]\n    E --&gt;|Writes| H[.wpilog Files]\n    F --&gt;|Prints| I[Console]</code></pre>","path":["Core Concepts"],"tags":[]},{"location":"core/overview/#components","level":2,"title":"Components","text":"","path":["Core Concepts"],"tags":[]},{"location":"core/overview/#1-logger","level":3,"title":"1. Logger","text":"<p>Singleton that manages the logging lifecycle and distributes data to receivers.</p> <pre><code>auto&amp; logger = tkit::Logger::GetInstance();  // (1)!\n\n// Lifecycle management\nlogger.Start();  // (2)!\nlogger.Periodic();  // (3)!\nlogger.End();\n</code></pre> <ol> <li>Returns the same singleton instance across the entire program</li> <li>Safe to call multiple times - won't reinitialize receivers</li> <li>Must be called every robot cycle to send data to receivers</li> </ol> <p>Responsibilities:</p> <ul> <li>Maintains the root <code>LogTable</code> with all logged values</li> <li>Manages receiver lifecycle (<code>OnStart</code>, <code>OnUpdate</code>, <code>OnEnd</code>)</li> <li>Provides both member functions and global convenience functions</li> <li>Tracks timestamps in microseconds using FPGA timestamp</li> </ul> <p>Recording data:</p> Member FunctionsGlobal Functions <pre><code>auto&amp; logger = tkit::Logger::GetInstance();\nlogger.RecordOutput(\"Speed\", 3.5);  // (1)!\nlogger.Periodic();\n</code></pre> <ol> <li>Call methods directly on the logger instance</li> </ol> <pre><code>tkit::RecordOutput(\"Speed\", 3.5);  // (1)!\ntkit::Periodic();\n</code></pre> <ol> <li>Convenience functions that call <code>Logger::GetInstance()</code> internally</li> </ol> <p>Choose Your Style</p> <p>Both approaches are functionally identical - use whichever you prefer! Member functions make the singleton dependency explicit, while global functions are more concise.</p>","path":["Core Concepts"],"tags":[]},{"location":"core/overview/#2-logvalue","level":3,"title":"2. LogValue","text":"<p>Type-safe wrapper that can hold any loggable value using <code>std::variant</code>.</p> <p>Supported types:</p> PrimitivesArraysStructs <pre><code>tkit::LogValue boolVal(true);  // (1)!\ntkit::LogValue intVal(42);\ntkit::LogValue floatVal(3.14f);\ntkit::LogValue doubleVal(2.718);\ntkit::LogValue stringVal(\"Hello\");\n</code></pre> <ol> <li>Type is automatically detected from the constructor argument</li> </ol> <pre><code>std::vector&lt;double&gt; speeds = {1.0, 2.0, 3.0};\ntkit::LogValue arrayVal(speeds);  // (1)!\n\nstd::vector&lt;std::string&gt; names = {\"FL\", \"FR\", \"BL\", \"BR\"};\ntkit::LogValue stringArray(names);\n</code></pre> <ol> <li>Works with any <code>std::vector</code> of supported types</li> </ol> <pre><code>frc::Pose2d pose{1.5_m, 2.3_m, frc::Rotation2d{45_deg}};\ntkit::LogValue structVal = tkit::MakeStructValue(pose);  // (1)!\n\n// Arrays of structs\nstd::vector&lt;frc::SwerveModuleState&gt; states = {...};\ntkit::LogValue structArray = tkit::MakeStructArrayValue(states);  // (2)!\n</code></pre> <ol> <li><code>MakeStructValue</code> serializes the struct and collects schemas</li> <li><code>MakeStructArrayValue</code> handles arrays of structs</li> </ol> <p>Runtime type information:</p> <pre><code>LogValue value(42);\n\nvalue.GetType();           // (1)!\nvalue.Is&lt;int64_t&gt;();       // (2)!\nvalue.Get&lt;int64_t&gt;();      // (3)!\nvalue.GetTypeString();     // (4)!\n</code></pre> <ol> <li>Returns <code>LogType::kInt64</code> enum</li> <li>Template type check - returns <code>true</code></li> <li>Extracts the value - returns <code>42</code></li> <li>Returns string representation - <code>\"int64\"</code></li> </ol> <p>Type Safety</p> <p>Use <code>Is&lt;T&gt;()</code> before <code>Get&lt;T&gt;()</code> to avoid exceptions. Calling <code>Get&lt;T&gt;()</code> with the wrong type throws <code>std::bad_variant_access</code>.</p>","path":["Core Concepts"],"tags":[]},{"location":"core/overview/#3-receivers","level":3,"title":"3. Receivers","text":"<p>Receivers implement the <code>LogDataReceiver</code> interface and process logged data.</p> <pre><code>class LogDataReceiver {\n public:\n  virtual void OnStart() = 0;  // (1)!\n  virtual void OnUpdate(const LogTable&amp; table, int64_t timestamp) = 0;  // (2)!\n  virtual void OnEnd() = 0;  // (3)!\n};\n</code></pre> <ol> <li>Called once when <code>Logger::Start()</code> is invoked - initialize resources, open files, create NT4 publishers</li> <li>Called every <code>Logger::Periodic()</code> - receives full table and microsecond timestamp, writes to files, publishes to NT4</li> <li>Called when <code>Logger::End()</code> is invoked - cleanup resources, close files, flush buffers</li> </ol> <p>Built-in receivers:</p> Receiver Purpose Output <code>NetworkTablesReceiver</code> Real-time dashboard viewing NT4 topics <code>WPILogWriter</code> Post-match analysis <code>.wpilog</code> files <code>ConsoleReceiver</code> Debugging Console output","path":["Core Concepts"],"tags":[]},{"location":"core/overview/#data-flow","level":2,"title":"Data Flow","text":"<pre><code>sequenceDiagram\n    participant Code as Robot Code\n    participant Logger\n    participant Table as LogTable\n    participant NT as NetworkTablesReceiver\n    participant Log as WPILogWriter\n\n    Code-&gt;&gt;Logger: RecordOutput(\"key\", value)\n    Logger-&gt;&gt;Table: Put(\"key\", value)\n\n    Code-&gt;&gt;Logger: Periodic()\n    Logger-&gt;&gt;NT: OnUpdate(table, timestamp)\n    NT-&gt;&gt;NT: Publish changed values to NT4\n    Logger-&gt;&gt;Log: OnUpdate(table, timestamp)\n    Log-&gt;&gt;Log: Append changed values to .wpilog</code></pre> <p>Two-Phase Logging</p> <p>Data flows in two phases: Record (store values in LogTable) and Flush (send to all receivers during <code>Periodic()</code>)</p>","path":["Core Concepts"],"tags":[]},{"location":"core/overview/#field-change-only-optimization","level":3,"title":"Field-Change-Only Optimization","text":"<p>Both <code>NetworkTablesReceiver</code> and <code>WPILogWriter</code> implement field-change-only optimization:</p> <pre><code>// First call - value is logged\ntkit::RecordOutput(\"Speed\", 3.5);  // (1)!\ntkit::Periodic();\n\n// Second call - same value, not logged\ntkit::RecordOutput(\"Speed\", 3.5);  // (2)!\ntkit::Periodic();\n\n// Third call - value changed, logged again\ntkit::RecordOutput(\"Speed\", 4.2);  // (3)!\ntkit::Periodic();\n</code></pre> <ol> <li>First value recorded → receivers publish/write this value</li> <li>Same value as before → receivers detect no change and skip processing (optimization!)</li> <li>Value changed from 3.5 to 4.2 → receivers publish/write the new value</li> </ol>","path":["Core Concepts"],"tags":[]},{"location":"core/overview/#next-steps","level":2,"title":"Next Steps","text":"<ul> <li>Learn about LogValue in detail</li> <li>Explore Receivers and how to configure them</li> <li>See Struct Logging for working with WPILib structs</li> </ul>","path":["Core Concepts"],"tags":[]},{"location":"examples/basic/","level":1,"title":"Basic Examples","text":"<p>This page provides practical examples of using TelemetryKit in common FRC robot scenarios.</p>","path":["Examples","Basic Examples"],"tags":[]},{"location":"examples/basic/#minimal-setup","level":2,"title":"Minimal Setup","text":"<p>The simplest possible TelemetryKit setup:</p> Robot.cpp<pre><code>#include &lt;frc/TimedRobot.h&gt;\n#include &lt;telemetrykit/TelemetryKit.h&gt;\n\nclass Robot : public frc::TimedRobot {\n public:\n  void RobotInit() override {\n    auto&amp; logger = tkit::Logger::GetInstance();\n    logger.AddReceiver(std::make_unique&lt;tkit::NetworkTablesReceiver&gt;());\n    logger.Start();\n  }\n\n  void RobotPeriodic() override {\n    tkit::Logger::RecordOutput(\"Timestamp\", tkit::Logger::GetTimestampUs());\n    tkit::Logger::Periodic();\n  }\n};\n</code></pre>","path":["Examples","Basic Examples"],"tags":[]},{"location":"examples/basic/#drivetrain-telemetry","level":2,"title":"Drivetrain Telemetry","text":"<p>Log motor speeds, encoder positions, and gyro angle:</p> Drivetrain.h<pre><code>#include &lt;frc/motorcontrol/PWMSparkMax.h&gt;\n#include &lt;frc/Encoder.h&gt;\n#include &lt;AHRS.h&gt;\n#include &lt;telemetrykit/TelemetryKit.h&gt;\n\nclass Drivetrain {\n public:\n  void UpdateTelemetry() {\n    // Motor speeds\n    tkit::Logger::RecordOutput(\"Drive/LeftSpeed\", m_leftMotor.Get());\n    tkit::Logger::RecordOutput(\"Drive/RightSpeed\", m_rightMotor.Get());\n\n    // Encoder positions\n    tkit::Logger::RecordOutput(\"Drive/LeftPosition\", m_leftEncoder.GetDistance());\n    tkit::Logger::RecordOutput(\"Drive/RightPosition\", m_rightEncoder.GetDistance());\n\n    // Gyro angle\n    tkit::Logger::RecordOutput(\"Drive/GyroAngle\", m_gyro.GetAngle());\n\n    // Battery voltage\n    tkit::Logger::RecordOutput(\"Drive/BatteryVoltage\",\n      frc::RobotController::GetBatteryVoltage());\n  }\n\n private:\n  frc::PWMSparkMax m_leftMotor{0};\n  frc::PWMSparkMax m_rightMotor{1};\n  frc::Encoder m_leftEncoder{0, 1};\n  frc::Encoder m_rightEncoder{2, 3};\n  AHRS m_gyro{frc::SPI::kMXP};\n};\n</code></pre>","path":["Examples","Basic Examples"],"tags":[]},{"location":"examples/basic/#swerve-drive-with-odometry","level":2,"title":"Swerve Drive with Odometry","text":"<p>Complete swerve drive telemetry including pose estimation:</p> SwerveDrive.cpp<pre><code>#include &lt;frc/geometry/Pose2d.h&gt;\n#include &lt;frc/kinematics/SwerveDriveOdometry.h&gt;\n#include &lt;telemetrykit/TelemetryKit.h&gt;\n#include &lt;telemetrykit/core/StructLogger.h&gt;\n\nclass SwerveDrive {\n public:\n  void UpdateTelemetry() {\n    auto&amp; table = tkit::Logger::GetTable();\n\n    // Robot pose (multiple formats for different uses)\n    frc::Pose2d pose = m_odometry.GetPose();\n\n    // Full struct for AdvantageScope visualization\n    tkit::Logger::RecordOutput(\"Swerve/Pose\", tkit::MakeStructValue(pose));\n\n    // Decomposed for dashboard graphing\n    tkit::LogPose2d(table, \"Swerve/PoseFields\", pose);\n\n    // Module states (decomposed)\n    tkit::LogSwerveModuleStates(table, \"Swerve/MeasuredStates\", m_measuredStates);\n    tkit::LogSwerveModuleStates(table, \"Swerve/DesiredStates\", m_desiredStates);\n\n    // Module states as struct arrays for AdvantageScope\n    tkit::Logger::RecordOutput(\"Swerve/MeasuredStatesStruct\",\n      tkit::MakeStructArrayValue(std::span(m_measuredStates)));\n    tkit::Logger::RecordOutput(\"Swerve/DesiredStatesStruct\",\n      tkit::MakeStructArrayValue(std::span(m_desiredStates)));\n\n    // Gyro angle\n    tkit::Logger::RecordOutput(\"Swerve/GyroAngle\", m_gyro.GetRotation2d().Degrees().value());\n  }\n\n private:\n  frc::SwerveDriveOdometry&lt;4&gt; m_odometry;\n  std::array&lt;frc::SwerveModuleState, 4&gt; m_measuredStates;\n  std::array&lt;frc::SwerveModuleState, 4&gt; m_desiredStates;\n  AHRS m_gyro{frc::SPI::kMXP};\n};\n</code></pre>","path":["Examples","Basic Examples"],"tags":[]},{"location":"examples/basic/#vision-system","level":2,"title":"Vision System","text":"<p>Log AprilTag detections and pose estimates:</p> VisionSystem.cpp<pre><code>#include &lt;photon/PhotonCamera.h&gt;\n#include &lt;telemetrykit/TelemetryKit.h&gt;\n\nclass VisionSystem {\n public:\n  void UpdateTelemetry() {\n    auto result = m_camera.GetLatestResult();\n\n    // Detection info\n    tkit::Logger::RecordOutput(\"Vision/HasTargets\", result.HasTargets());\n    tkit::Logger::RecordOutput(\"Vision/TargetCount\", static_cast&lt;int64_t&gt;(result.GetTargets().size()));\n    tkit::Logger::RecordOutput(\"Vision/Latency\", result.GetLatency());\n\n    if (result.HasTargets()) {\n      auto target = result.GetBestTarget();\n\n      // Target data\n      tkit::Logger::RecordOutput(\"Vision/BestTarget/ID\", target.GetFiducialId());\n      tkit::Logger::RecordOutput(\"Vision/BestTarget/Yaw\", target.GetYaw());\n      tkit::Logger::RecordOutput(\"Vision/BestTarget/Pitch\", target.GetPitch());\n      tkit::Logger::RecordOutput(\"Vision/BestTarget/Area\", target.GetArea());\n\n      // Robot pose from vision\n      auto poseEstimate = m_poseEstimator.Update(result);\n      if (poseEstimate.has_value()) {\n        tkit::Logger::RecordOutput(\"Vision/EstimatedPose\",\n          tkit::MakeStructValue(poseEstimate.value().estimatedPose.ToPose2d()));\n      }\n    }\n  }\n\n private:\n  photon::PhotonCamera m_camera{\"PhotonVision\"};\n  photon::PhotonPoseEstimator m_poseEstimator;\n};\n</code></pre>","path":["Examples","Basic Examples"],"tags":[]},{"location":"examples/basic/#subsystem-io-pattern","level":2,"title":"Subsystem IO Pattern","text":"<p>AdvantageKit-style IO pattern with TelemetryKit:</p> ElevatorIO.h<pre><code>#include &lt;telemetrykit/core/LoggableInputs.h&gt;\n\nstruct ElevatorIOInputs : public tkit::LoggableInputs {\n  double positionMeters = 0.0;\n  double velocityMetersPerSecond = 0.0;\n  double appliedVolts = 0.0;\n  double currentAmps = 0.0;\n\n  void ToLog(tkit::LogTable&amp; table, std::string_view prefix) override {\n    std::string p(prefix);\n    table.Put(p + \"PositionMeters\", positionMeters);\n    table.Put(p + \"VelocityMetersPerSecond\", velocityMetersPerSecond);\n    table.Put(p + \"AppliedVolts\", appliedVolts);\n    table.Put(p + \"CurrentAmps\", currentAmps);\n  }\n};\n\nclass ElevatorIO {\n public:\n  virtual void UpdateInputs(ElevatorIOInputs&amp; inputs) = 0;\n  virtual void SetVoltage(double volts) = 0;\n};\n</code></pre> Elevator.cpp<pre><code>#include \"ElevatorIO.h\"\n#include &lt;telemetrykit/TelemetryKit.h&gt;\n\nclass Elevator {\n public:\n  void Periodic() {\n    // Update inputs\n    m_io-&gt;UpdateInputs(m_inputs);\n\n    // Log to TelemetryKit\n    tkit::Logger::ProcessInputs(\"Elevator\", m_inputs);\n\n    // Control logic...\n  }\n\n private:\n  std::unique_ptr&lt;ElevatorIO&gt; m_io;\n  ElevatorIOInputs m_inputs;\n};\n</code></pre>","path":["Examples","Basic Examples"],"tags":[]},{"location":"examples/basic/#auto-routine-tracking","level":2,"title":"Auto Routine Tracking","text":"<p>Track which auto routine is selected and running:</p> Robot.cpp<pre><code>class Robot : public frc::TimedRobot {\n public:\n  void AutonomousInit() override {\n    std::string selectedAuto = m_chooser.GetSelected();\n    tkit::Logger::RecordOutput(\"Auto/Selected\", selectedAuto);\n    tkit::Logger::RecordOutput(\"Auto/StartTime\", tkit::Logger::GetTimestampUs());\n\n    m_autoCommand = GetAutoCommand(selectedAuto);\n    m_autoCommand-&gt;Schedule();\n  }\n\n  void AutonomousPeriodic() override {\n    // Log command states\n    auto scheduler = frc2::CommandScheduler::GetInstance();\n    auto runningCommands = GetRunningCommandNames(scheduler);\n\n    tkit::Logger::RecordOutput(\"Auto/RunningCommands\", runningCommands);\n    tkit::Logger::Periodic();\n  }\n\n private:\n  frc::SendableChooser&lt;std::string&gt; m_chooser;\n  frc2::CommandPtr m_autoCommand;\n};\n</code></pre>","path":["Examples","Basic Examples"],"tags":[]},{"location":"examples/basic/#diagnostics-and-health-monitoring","level":2,"title":"Diagnostics and Health Monitoring","text":"<p>Monitor robot health metrics:</p> Diagnostics.cpp<pre><code>#include &lt;frc/RobotController.h&gt;\n#include &lt;telemetrykit/TelemetryKit.h&gt;\n\nclass Diagnostics {\n public:\n  void UpdateTelemetry() {\n    // Power\n    tkit::Logger::RecordOutput(\"Diagnostics/BatteryVoltage\",\n      frc::RobotController::GetBatteryVoltage());\n    tkit::Logger::RecordOutput(\"Diagnostics/3v3Rail\",\n      frc::RobotController::GetVoltage3V3());\n    tkit::Logger::RecordOutput(\"Diagnostics/5vRail\",\n      frc::RobotController::GetVoltage5V());\n    tkit::Logger::RecordOutput(\"Diagnostics/6vRail\",\n      frc::RobotController::GetVoltage6V());\n\n    // FMS\n    tkit::Logger::RecordOutput(\"Diagnostics/FMSAttached\",\n      frc::DriverStation::IsFMSAttached());\n    tkit::Logger::RecordOutput(\"Diagnostics/MatchTime\",\n      frc::DriverStation::GetMatchTime());\n  }\n\n private:\n  double GetFreeRAM();\n  double GetCPUTemperature();\n};\n</code></pre> <p>Advanced Topics →</p>","path":["Examples","Basic Examples"],"tags":[]},{"location":"receivers/overview/","level":1,"title":"Receivers","text":"<p>Receivers process logged data from the <code>LogTable</code> during each <code>Logger::Periodic()</code> call. They implement the <code>LogDataReceiver</code> interface with three lifecycle methods: <code>OnStart()</code>, <code>OnUpdate()</code>, and <code>OnEnd()</code>.</p> <p>TelemetryKit includes three built-in receivers for different output targets.</p>","path":["Core Concepts","Receivers"],"tags":[]},{"location":"receivers/overview/#built-in-receivers","level":2,"title":"Built-in Receivers","text":"","path":["Core Concepts","Receivers"],"tags":[]},{"location":"receivers/overview/#networktablesreceiver","level":3,"title":"NetworkTablesReceiver","text":"<p>Publishes data to NetworkTables 4 for real-time dashboard viewing.</p> <pre><code>#include &lt;telemetrykit/receiver/NetworkTablesReceiver.h&gt;\n\nlogger.AddReceiver(std::make_unique&lt;tkit::NetworkTablesReceiver&gt;());\n</code></pre> <p>What it does:</p> <ul> <li>Publishes all LogValue types to NT4 topics</li> <li>Publishes struct schemas</li> <li>Only sends changed values</li> </ul> <p>Configuration:</p> <pre><code>// Use a custom NT instance (optional)\nauto inst = nt::NetworkTableInstance::Create();\nauto receiver = std::make_unique&lt;tkit::NetworkTablesReceiver&gt;(inst);\n</code></pre> <p>Works with Shuffleboard, Glass, AdvantageScope, and Elastic.</p>","path":["Core Concepts","Receivers"],"tags":[]},{"location":"receivers/overview/#wpilogwriter","level":3,"title":"WPILogWriter","text":"<p>Writes data to <code>.wpilog</code> files for post-match analysis.</p> <pre><code>#include &lt;telemetrykit/receiver/WPILogWriter.h&gt;\n\nlogger.AddReceiver(std::make_unique&lt;tkit::WPILogWriter&gt;(\"/home/lvuser/logs\"));\n</code></pre> <p>What it does:</p> <ul> <li>Writes to timestamped <code>.wpilog</code> files</li> <li>Serializes structs with proper type strings</li> <li>Only writes changed values</li> </ul> <p>Files are saved as <code>TelemetryKit_YYYYMMDD_HHMMSS.wpilog</code> and can be viewed in AdvantageScope.</p>","path":["Core Concepts","Receivers"],"tags":[]},{"location":"receivers/overview/#consolereceiver","level":3,"title":"ConsoleReceiver","text":"<p>Prints data to console for debugging.</p> <pre><code>#include &lt;telemetrykit/receiver/ConsoleReceiver.h&gt;\n\nlogger.AddReceiver(std::make_unique&lt;tkit::ConsoleReceiver&gt;());\n</code></pre> <p>What it does:</p> <ul> <li>Prints human-readable output</li> <li>Only prints changed values by default</li> </ul> <p>Configuration:</p> <pre><code>// Custom prefix\nauto receiver = std::make_unique&lt;tkit::ConsoleReceiver&gt;(\"[MyRobot]\");\n\n// Print all values every cycle\nreceiver-&gt;SetPrintOnlyChanges(false);\n</code></pre> <p>Warning</p> <p>For debugging only. Disable on competition robots.</p>","path":["Core Concepts","Receivers"],"tags":[]},{"location":"receivers/overview/#using-multiple-receivers","level":2,"title":"Using Multiple Receivers","text":"<p>You can use multiple receivers simultaneously:</p> <pre><code>auto&amp; logger = tkit::Logger::GetInstance();\n\n// Real-time dashboard viewing\nlogger.AddReceiver(std::make_unique&lt;tkit::NetworkTablesReceiver&gt;());\n\n// Post-match analysis\nlogger.AddReceiver(std::make_unique&lt;tkit::WPILogWriter&gt;(\"/home/lvuser/logs\"));\n\n// Debug output (disable on competition robot)\n#ifdef DEBUG\nlogger.AddReceiver(std::make_unique&lt;tkit::ConsoleReceiver&gt;());\n#endif\n\nlogger.Start();\n</code></pre> <p>Tip</p> <p>Receivers operate independently. If one fails, others continue.</p>","path":["Core Concepts","Receivers"],"tags":[]},{"location":"receivers/overview/#receiver-lifecycle","level":2,"title":"Receiver Lifecycle","text":"<p>All receivers implement three lifecycle methods:</p> <pre><code>graph LR\n  Start([Start]) --&gt;|Logger::Start| OnStart[OnStart]\n  OnStart --&gt;|Logger::Periodic| OnUpdate[OnUpdate]\n  OnUpdate --&gt;|Logger::Periodic| OnUpdate\n  OnUpdate --&gt;|Logger::End| OnEnd[OnEnd]\n  OnEnd --&gt; End([End])</code></pre> <p><code>OnStart()</code> - Called once when <code>Logger::Start()</code> is invoked. Use for initialization (open files, create publishers, allocate resources).</p> <p><code>OnUpdate(const LogTable&amp; table, int64_t timestamp)</code> - Called every <code>Logger::Periodic()</code> call. Process logged data from the table.</p> <p><code>OnEnd()</code> - Called when <code>Logger::End()</code> is invoked. Use for cleanup (close files, flush buffers, release resources).</p>","path":["Core Concepts","Receivers"],"tags":[]},{"location":"receivers/overview/#custom-receivers","level":2,"title":"Custom Receivers","text":"<p>Implement the <code>LogDataReceiver</code> interface:</p> <pre><code>#include &lt;telemetrykit/receiver/LogDataReceiver.h&gt;\n\nclass CustomReceiver : public tkit::LogDataReceiver {\n public:\n  void OnStart() override {\n    // Initialize resources\n    std::cout &lt;&lt; \"Custom receiver started!\" &lt;&lt; std::endl;  // (1)!\n  }\n\n  void OnUpdate(const tkit::LogTable&amp; table, int64_t timestamp) override {\n    // Process data\n    auto entries = table.GetAllEntries();\n    for (const auto&amp; [key, value] : entries) {\n      // Do something with key and value\n      ProcessEntry(key, value, timestamp);  // (2)!\n    }\n  }\n\n  void OnEnd() override {\n    // Cleanup\n    std::cout &lt;&lt; \"Custom receiver stopped!\" &lt;&lt; std::endl;  // (3)!\n  }\n\n private:\n  void ProcessEntry(const std::string&amp; key, const tkit::LogValue&amp; value, int64_t timestamp) {\n    // Your custom logic here\n  }\n};\n</code></pre> <ol> <li>Open files, connect to databases, or initialize network connections</li> <li>Process each entry - use <code>value.GetType()</code> and <code>value.Get&lt;T&gt;()</code> to extract data</li> <li>Close connections and flush buffers before returning</li> </ol>","path":["Core Concepts","Receivers"],"tags":[]},{"location":"receivers/overview/#performance-considerations","level":2,"title":"Performance Considerations","text":"","path":["Core Concepts","Receivers"],"tags":[]},{"location":"receivers/overview/#change-only-optimization","level":3,"title":"Change-Only Optimization","text":"<p><code>NetworkTablesReceiver</code> and <code>WPILogWriter</code> only process changed values:</p> <pre><code>// Internal tracking\nstd::unordered_map&lt;std::string, LogValue&gt; m_lastValues;  // (1)!\n\n// Only publish/write if value changed\nif (m_lastValues[key] != value) {  // (2)!\n  PublishValue(key, value, timestamp);\n  m_lastValues[key] = value;  // (3)!\n}\n</code></pre> <ol> <li>Each receiver maintains a cache of last-seen values</li> <li>Compares current value against cached value using <code>LogValue::operator==</code></li> <li>Updates cache only when value changes</li> </ol> <p>Next: Struct Logging →</p>","path":["Core Concepts","Receivers"],"tags":[]}]}